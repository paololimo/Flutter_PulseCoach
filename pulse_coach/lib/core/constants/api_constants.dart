// API URLs and compile-time constants — populated in Story 3.x, 4.x, 6.x
// ignore_for_file: avoid_classes_with_only_static_members — intentional: ApiConstants groups related compile-time constants by domain

class ApiConstants {
  // Open-Meteo API (Story 4.1)
  // ExerciseDB API (Story 6.1)
}
