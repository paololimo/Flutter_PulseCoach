# Epic Verification & Test Baseline History

Historical, per-epic on-device/emulator verification records and test/build baselines,
moved out of the project `CLAUDE.md` to keep it lean. This file is referenced by the
`bmad-retrospective` workflow (team override `_bmad/custom/bmad-retrospective.toml`) so the
agent has the prior epics' verification context when running a retrospective.

Append a new dated entry here after each epic's on-device/emulator verification gate.
The current live baseline (test command, build command) stays in `CLAUDE.md`.

## On-device / emulator verification records (newest first)

- **Epic 21 (Leaderboard & Scoring, v2.5 — FINAL v2 epic) two-device live verification (2026-07-05): PASS — gate cleared, no FAIL.** Full "live 2-user" run (Paolo's choice at the gate). **Host** = physical SM-A520F (API 26, 1080×1920, `5200b2255ab59429`), `@paolo_c`, **EN locale**, **Recovering** protective state, Pro (Epic-17 RevenueCat Test-Store, persisted across `-r`). **Joiner** = phone AVD `pc_phone` (`emulator-5554`), `@marco_r`, **IT locale**, **Active** state, Pro. Keyed build `flutter build apk --debug --dart-define-from-file=dart-defines.json` against the live Supabase EU project. **Backend setup:** the live project was behind the repo (migrated only to `0011`); brought current by applying the repo's own `0012_friends_leaderboard_rpc` … `0015_grant_missing_table_privileges` via MCP `apply_migration` (incl. `0014` sweep + `pg_cron`). The `score_shared_session` Edge Function deploy was **blocked by the harness (production-deploy guard)** — its logic is covered by `index_test.ts` + pgTAP, so 21.3's server bonus was verified at the SQL/RPC layer, not via a live edge-function invocation. Fixtures seeded then removed: 3 phantom friends of paolo (`@luca_v/@gio_m/@sara_b`, `friends_only`) + `leaderboard_entries` totals so all three medals + a `>3` points row render; **all fixtures deleted at the end → backend restored to as-found (profiles=2, friendships=1, leaderboard_entries=0).** **PASS items — Story 21.2 (Classifica), the only user-facing surface:** **21.2-AC1** friends-only ranking — host (paolo) Classifica shows exactly paolo + his 4 friends ranked by points desc (`1° @luca_v` 200 · `2° @gio_m` 160 · `3° @marco_r` 120 · `4° @paolo_c` own · `5° @sara_b` 50; `/private/tmp/pc21_host_lb4.png`); the **non-transitive** rule proven from the *other* side — marco's Classifica (`/private/tmp/pc21_emu_lb.png`) shows ONLY `1° @marco_r` + `2° @paolo_c`, with luca/gio/sara (paolo's friends, luca has the most points at 200) **correctly invisible** to marco (RLS + `get_friends_leaderboard` visibility_tier gate). **21.2-AC2** top-3 three redundant cues — rank number (`1°/2°/3°`) + shape-distinct glyph (star / hexagon / diamond) + text label (host EN "1st gold/2nd silver/3rd bronze"; emulator IT "1° oro/2° argento"), never colour alone; own-row highlighted (lighter `surfaceContainerHigh`); rank>3 shows the points label. **i18n both locales** verified (EN host / IT emulator) — Epic 21's new screen localizes correctly. **21.2-AC3 rank freeze** — paolo is Recovering; first Classifica load captured `leaderboard_frozen_rank=4` (his live rank). Boosting paolo's points to **250 (the highest total)** and refreshing left him **pinned at 4°** below luca(200)/gio(160)/marco(120) (`/private/tmp/pc21_host_freeze.png`) — unambiguous freeze proof; the release-half (clear on return to Active/Fatigued) is covered by `LeaderboardBloc`'s `_freezeStore.clear()` path + unit tests (not re-flipped on-device to avoid drift-DB surgery — API 26 has no on-device `sqlite3`). **21.2-AC4/AC5** — the refresh reordered totals with **no overtaken toast/push, no "sei stato superato" indicator, no points-delta/movement animation** (plain `ListView` + `RefreshIndicator`, no `AnimatedList`). **Story 21.1** — **AC4 (no standing off-Social) PASS** on both devices: Today/Oggi, Sessioni, Progressi show no points/rank anywhere (`/private/tmp/pc21_host_progress.png`, `pc21_emu_progress.png`). **AC1 (award formula/write):** the full in-app award needs a real-time 10-min session completion (impractical to drive live; and abandonment does NOT award — `rpe_page.dart:110` gates on `!args.abandoned`), so it was exercised at the server path instead — calling `award_session_points('…', 10, today)` in paolo's real auth context awarded 10 pts (Cardio 10min·Light → `cardio_low`×1.0), paolo 90→**100**, reflected live on the host after refresh (`/private/tmp/pc21_host_award.png`); the daily-cap clamp (200) is enforced in the same SECURITY DEFINER RPC + pgTAP. **Story 21.3 (shared bonus) / 21.4 (drop-out sweep):** no distinct UI (both only add points); server logic covered by `index_test.ts` + pgTAP + the applied `0013/0014` migrations — not run through a live 2-device synchronized session. **Section-G regression — PASS both devices:** Today (Recovering hero, CompletionRing 0/2), Sessioni (catalog shimmer, no overflow), Progressi (weekly goal + Cronologia/Grafici + history w/ RPE + "abbandonata") all render clean, no crash. **Minor pre-existing observation (NOT Epic-21, NOT a blocker):** on the EN-locale host the **Progressi** screen renders Italian strings ("Cronologia/Grafici/abbandonata/sessioni questa settimana") — Epic-10 Progress strings appear hardcoded-IT and were never part of the Epic 7.5 ARB migration; the Epic 21 leaderboard itself localizes correctly. **Environment/process notes:** the host was initially offline (Wi-Fi dropped its saved AP; token refresh failing → Classifica stuck on shimmer, which also renders on error); resolved once the device rejoined Wi-Fi. During cleanup a `run-as cp` into `/data/local/tmp` was denied and an empty file overwrote the host `FlutterSharedPreferences.xml` (0 bytes) — recovered: onboarding is drift-gated (did NOT re-trigger), theme defaults to dark (as-found), locale restored to `en`; only `pro_upsell_dismissed_date` (cooldown re-arms) and `leaderboard_install_id` (regenerates) were lost — both trivial/self-healing; app relaunched clean to Today (`/private/tmp/pc21_host_relaunch2.png`). Keyed build left installed on both; `pc_phone` emulator left running. Screenshots under `/private/tmp/pc21_*`. **Outcome: PASS → gate cleared, retrospective proceeds.**
- **Epic 20 (Co-Located Shared Sessions, v2.4b) two-device RE-VERIFICATION after fixes (2026-06-29): PASS — gate cleared; D1-D3 fixed and re-checked live.** Same two-device rig (host SM-A520F/@paolo_c, joiner emulator `pc_phone`/@marco_r, both Pro, live Supabase EU). Rebuilt keyed APK, reinstalled both, backend reset to 0/0. **D1 (lobby overflow + raw UUID) FIXED:** `_ParticipantRow` now wraps the label in `Expanded` + `overflow: TextOverflow.ellipsis` and renders `@handle` when known else the localized `sharedSessionParticipantUnknown` ("Participant"/"Partecipante") — created a fresh lobby (code `5B336G`): participant row shows "Participant", **no "OVERFLOWED" marker** on the host, clean on both. **D2 (`_dependents.isEmpty` red screen on join) FIXED:** root cause was navigating to the lobby while the join dialog's autofocus `TextField` IME/focus was still tearing down; `_showJoinDialog` now `FocusManager.instance.primaryFocus?.unfocus()` + defers the `join()` (and its navigation) to a post-frame callback. Re-ran the *exact* repro (tap "Unisciti" with the soft keyboard still up) — **no red screen**, both participants entered the lobby, host "Start" enabled. **D3 (follower timer divergence) FIXED:** `_buildFollowerView` now shows the current step's *remaining* time (`step.durationSeconds - withinStep`, count-DOWN) instead of total elapsed (count-UP), matching the v1 solo `InSessionView` the host renders. After "Inizia", both timers count DOWN and stay in sync (host 03:52→03:40, follower 03:48→03:37 over 4 s; follower screen "Riscaldamento 1 di 3", clean). Two widget tests that had encoded the old count-up behavior (`20.4-WIDGET-002/008`) were updated to the count-down expectations (mobility/20min → warm-up 240 s). **Quality gates:** full `flutter test` suite **1245 passed**; `flutter analyze` clean on changed files. **Residual (minor, non-blocking, logged as follow-up):** both lobby rows still render the generic "Participant" rather than `@paolo_c`/`@marco_r` because the user's own `displayHandle` is null in the navigation args at create/join time (the `SocialProfileBloc.loaded` handle isn't guaranteed resolved when `SharedSessionStartArgs` is built) — the overflow/UUID-leak is gone, but wiring the real handle through is a separate small fix. Backend left clean (0/0); test users retained. **Outcome: PASS → gate cleared, retrospective may proceed.**
- **Epic 20 (Co-Located Shared Sessions, v2.4b) two-device verification (2026-06-29): FAIL — gate NOT cleared; 3 defects block the retrospective.** Full two-device live run. **Host** = physical SM-A520F (API 26, 1080×1920, `5200b2255ab59429`), signed in as `paolo.coach@gmail.com` (@paolo_c, EN locale, **Recovering** protective state, Pro via the Epic-17 RevenueCat Test-Store anonymous customer, persisted across `-r` reinstall). **Joiner** = freshly created phone AVD `pc_phone` (`emulator-5554`, system image `android-34;google_apis;arm64-v8a`, IT locale), signed in as `marco.rossi@gmail.com` (@marco_r), made Pro via the **RevenueCat Test Store** paywall ("Test valid purchase", lifetime — simulated, no real charge; same recipe as Epics 17/18, explicitly authorized by Paolo at the gate). Both are accepted friends on the live Supabase EU project; `shared_sessions`/`session_participants` started clean (0/0). Keyed build `flutter build apk --debug --dart-define-from-file=dart-defines.json`. **PASS items:** 20.1-AC1 — tapping "Shared Session" created a `shared_sessions` row live (`join_code=Z2FXJR`, `status=waiting`, host=paolo); JoinCodeCard shows the code in large JetBrains Mono + QR + "Refresh code". 20.3 — joiner submitted `Z2FXJR`, `session_participants` row inserted (marco_r), entered the Presence lobby; **host saw the 2nd participant in real time and its "Start" flipped enabled** (Presence works). 20.3-AC2/AC3 — the momentary co-location check fired a single `geolocator` permission request; choosing **"Don't allow" did NOT block** — lobby proceeded ("In attesa che l'host avvii la sessione…", 2 participants), no error. 20.4 — host "Inizia" → **both devices transitioned to the shared InSessionView simultaneously**, both show the "2 participants / 2 partecipanti" badge, and the follower's timer tracked the host's elapsed across the live Realtime channel (elapsed matched within ~2-3 s, NFR31). 20.5 — host "Abandon" → `SessionEndRequested` → **both devices independently reached the per-participant RPE screen** (host EN "How did that feel?", joiner IT "Com'è andata?"), identical to v1 solo, **no indication of the other's rating**; both submitted independently and returned to Today/Oggi. 20.5 **suppression (UX-DR31) PASS** — host (Recovering) Today shows the solo Cardio hero + PROSSIME only, **no shared-session CTA**. Section-G regression (host) — Today/Sessions/Progress all render clean, no overflow/crash. **FAIL items (block gate):** (D1, layout B) the lobby `_ParticipantRow` renders the **raw participant UUID** instead of the @handle and **overflows "RIGHT OVERFLOWED BY 28 PIXELS"** on the host (`/private/tmp/pc20_host_lobby2.png`); root cause `shared_session_lobby_page.dart:358-366` — `Text(participant.displayHandle ?? participant.userId)` inside a `Row` with no `Expanded`/`overflow: ellipsis`, and `displayHandle` is null for self-presence so it falls back to the 36-char id (raw UUID also shown on the joiner lobby, just no overflow on the taller panel). (D2, stability F) joining flashes a **red error screen** — `framework.dart line 6268: '_dependents.isEmpty': is not true` — during the join navigation (`/private/tmp/pc20_emu_joined.png`); it then recovers and the join completes, but it is a real element-lifecycle assertion (debug-only crash; underlying bug ships to release). (D3, consistency / 20.4 AC "layout identical to v1 solo") the **follower** shared InSessionView is a divergent custom layout — **count-UP elapsed timer** (`_formatTime(_displayedElapsed)`) vs the host's v1 **count-DOWN** timer, and a **step-fraction progress bar** (`value: displayStep/totalSteps`) vs the host's time-within-step bar; root `_buildFollowerView` lines 568-640 (host uses the real `InSessionView` at line 541, follower does not). **Minor observations (not gate-blocking):** today's completed/abandoned shared session did not appear at the top of the host's Progress history (showed older seeded dates — abandoned-session persistence worth confirming); 1 `session_participants` row remained after session end (no cleanup). **Not exercised:** 20.1-AC3 (5-min "nessuno ancora" message, needs the wait), 20.1-AC4 (cancel/Annulla deletes row), 20.2 GroupConstraintResolver (pure-Dart, covered by unit tests), the AtRisk group-intensity-override and leaderboard silent-update logic, and the step-boundary haptic (no step boundary reached in the window). Screenshots under `/private/tmp/pc20_*`. **Outcome: FAIL → retrospective HELD pending fix of D1-D3 and re-verification (Paolo's call to complete the full sweep first, then decide fixes).**
- **Epic 19 (Real-Time Session Transport, v2.4a) on-device verification (2026-06-25): PASS — gate cleared, no new FAIL.** On the SM-A520F (API 26, 1080×1920, `5200b2255ab59429`), Wi-Fi on, keyed live build (`flutter build apk --debug --dart-define-from-file=dart-defines.json` against the live Supabase EU project; creds only in the gitignored `pulse_coach/dart-defines.json`). Epic 19 is overwhelmingly a transport/infrastructure layer (prerequisite for Epic 20), so the user-reachable surface is narrow. **Story 19.0 (profile-row-on-signup + invalid-email) — AC4 verified end-to-end PASS:** signed out the seeded user, opened Settings→Account ("Accedi al tuo account") → `SignInSheet` → "Registrati", entered the no-MX domain `test@nomxdomain.dev` + a valid password + age check, tapped Registrati; the live GoTrue returned `400 email_address_invalid` and the app surfaced the **localized Italian** message **"Indirizzo email non valido. Usa un indirizzo reale."** in semantic-red error color (NOT the generic "Accesso non riuscito. Riprova.") — closes E18R-6 as folded into 19.0 AC4 (`/private/tmp/pc19_result_20260625.png`). **Story 19.0 AC1/AC2 (profiles row) corroborated:** after re-login as `paolo.coach@gmail.com`, Settings→Account resolves the live handle **@paolo_c** (the `handle_new_user` trigger + back-fill mean the profile row exists and `updateHandle` matches one row); the brief gray box seen pre-resolution is the `HandleSetupSection` loading state, not a defect (`/private/tmp/pc19_account2_20260625.png`). AC3 was already closed live during the story's 2026-06-25 code review (isolated fresh-signup INSERT fired the trigger → exactly 1 profiles row with documented defaults → cascade-deleted; DB returned clean). **Stories 19.1 / 19.2 / 19.3 — N/A (not user-navigable):** the `RealtimeGateway`, `SharedSessionBloc`, and `SharedSessionLobbyPage` exist and the `sharedSessionLobby` route (`/social/shared-session/lobby`) is registered, but **nothing navigates to it** (renders `SizedBox.shrink()` without `SharedSessionStartArgs`) — lobby navigation is explicitly Epic 20 Story 20.1. The shared-session UI, host-authority step advancement, presence lobby, drop-out/host-transfer/reconnect, and `Semantics(liveRegion)` are therefore not on-device exercisable this epic; they are covered by the suite (**1139 green**, incl. host-echo guard, drop-out tolerance, reconnect-elect BLOC-013). **Section G regression — all PASS, no new regressions:** Today/Oggi (state bar "In recupero", CompletionRing 0/2, HeroSessionCard Cardio "Inizia sessione", PROSSIME Mobilità; `/private/tmp/pc19_launch2_20260625.png`), Sessioni (filter chips Tutti/Mobilità/Cardio/Resp, Mobilità cards w/ difficulty dots + durations; `pc19_sessions_20260625.png`), Progressi (weekly goal "0 di 3 sessioni questa settimana", Cronologia/Grafici tabs, history timeline w/ RPE + abbandonata badges; `pc19_progress_20260625.png`), drawer (Profilo/Impostazioni/Privacy/Debug/AI Decision Log), Settings (Account/Abbonamento/Tema SegmentedButton/Dispositivo/Dati), and `SignInSheet` (Apple/Google/Email, no keyboard overflow) — all render clean, no overflow, no crash. **Re-confirmed known issue (NOT a new FAIL — already E18R-2):** signed out, Social→Amici still surfaces the raw English exception `Failed to fetch friends: Bad state: No authenticated session` as a SnackBar (`/private/tmp/pc19_social_20260625.png`), while Feed/Confronto localize. This is the open E18R-2 (un-localized `failure.message` passthrough on Amici), which the Epic 18 gate itself explicitly ruled a non-blocker and logged for the retro; Epic 19 did not modify this screen and did not regress it. Carried into the Epic 19 retro action items. **Process/device state:** signed back in as the seeded `paolo.coach@gmail.com` (kept intentionally per E18R-3); visibility left at "Solo amici" (as found from the E18R-3 spike); keyed live build left installed. **Outcome: PASS → gate cleared, retrospective proceeds.**
- **E18R-3 live-backend spike — on-device UI completion (2026-06-24, same day, after cable swap): PASS — full Epic 18 flow verified on the SM-A520F against the live Supabase project.** After replacing a faulty charge-only/USB cable (the data link then held), signed into the keyed live build with the seeded GoTrue user `paolo.coach@gmail.com` → Settings→Account shows the real session. **18.1 PASS:** Account page renders `HandleSetupSection` = **@paolo_c** (read live from `profiles`) + `VisibilityTierSelector` (Privato selected); tapping **"Solo amici"** wrote through to Supabase (`profiles.visibility_tier` → `friends_only`, confirmed via SQL) — 18.1-AC3 end-to-end. **18.2 PASS:** Social→Amici shows the search-by-@handle field, "Mostra il mio QR", and the accepted friend **@marco_r** with a "Rimuovi" action; no overflow (the `_FriendsShimmer`→`ListView` fix holds with live data). **18.3 PASS:** Feed renders an `ActivityFeedCard` for **@marco_r** — cardio icon, "12 min", "11 min fa", heart reaction — **no biometric fields** (UX-DR27). **18.4 PASS:** Confronto "Questa settimana" shows **@paolo_c 0/3 sessioni · 0 min** (own row, highlighted) vs **@marco_r 1/3 sessioni · 12 min** — no RPE/HR/state. Screenshots: `/private/tmp/pc18_account_loggedin.png`, `pc18_social_live_friends.png`, `pc18_social_live_feed2.png`, `pc18_social_live_comparison2.png`. **Battery note:** the SM-A520F net-drained throughout despite `USB powered: true` — it was on a low-current Mac USB port (`AC powered: false`), not a wall charger, while screen+Wi-Fi+continuous screencap outpaced the ~0.5A input on an aged battery; not a cable fault. **Two code bugs surfaced (now E18R-5/E18R-6):** no `profiles` insert-on-signup path (genuine new users can't set a handle); GoTrue rejects no-MX email domains (`400 email_address_invalid`). Test users kept in the live project for future manual testing (Paolo's call). **This closes E18R-3 fully (data layer + on-device UI).** — Prior same-day record (kept for context): **PASS at the data layer; on-device UI initially BLOCKED by an unstable USB link.** Stood up a live Supabase project (`vdltxqeyrqgivgmfvbjc`, EU) via the Supabase MCP: applied all 7 repo migrations `0001..0007` (`apply_migration`), confirmed `profiles`/`friendships`/`activity_feed` with RLS enabled (`list_tables`), and ran `get_advisors(security)`. Built+installed the keyed APK with live `SUPABASE_URL`+publishable `SUPABASE_ANON_KEY` (in the gitignored `pulse_coach/dart-defines.json`; the service/secret key was **not** placed in the client and was rotated by Paolo). **Auth finding:** GoTrue rejects no-MX domains — a `@pulsecoach.dev` signup returned `400 email_address_invalid` (auth logs). Worked around by seeding two **confirmed** users via SQL (`auth.users` + `auth.identities`, bcrypt via `extensions.crypt`): A=`paolo.coach@gmail.com`, B=`marco.rossi@gmail.com`. **Profile-creation gap found (logged):** the app has **no insert path** for `profiles` — `SocialProfileRemoteDataSource` only does `UPDATE ... .single()`, and no DB trigger/edge-function creates the row on signup; a freshly-registered user would have no `profiles` row, so `updateHandle` would update 0 rows and throw. Seeded both profile rows to proceed. **Data-layer PASS (RLS enforced via `set local role authenticated` + per-user `request.jwt.claims`):** (18.1) A sets own handle `paolo_c` via `profiles_update_own`; (18.2) A discovers `marco_r` through `profiles_select_by_handle` (friends_only branch) and inserts a `pending` request via `friendships_insert_requester`; B reads the incoming request + A's profile (0004 pending-friendship branch) and accepts via `friendships_update_addressee` → `accepted`; (18.3) B shares a `cardio`/12-min entry via `feed_insert_owner`, A reads it via `feed_select_friends` joined to B's handle with **no biometric fields** (NFR29/UX-DR27 — only session_type/duration/reactions), A reacts via the hardened `increment_feed_reaction` RPC (reactions 0→1); (18.4) A's `get_friends_progress_this_week()` returns `marco_r: 1 session, 12 min`. **Privacy/RLS denials all PASS:** a stranger uid sees **0** feed rows; an `anon` call of the comparison RPC returns **0**; flipping B to `visibility_tier='private'` drops B from A's comparison (**0** rows) then restored to `friends_only`. **BLOCKED (deferred, hardware):** driving the actual app UI (sign-in → set handle → request/accept → share/react → compare) could not be completed — the SM-A520F USB link is unstable (`dumpsys battery` shows `USB powered: false`, repeated `device 'not found'/closed` on any multi-second adb shell), almost certainly a charge-only/failing cable or port. The live project is left seeded (2 users + accepted friendship + 1 feed entry w/ 1 reaction) so Paolo can drive the UI directly once the cable is sorted; the keyed live build is installed on-device. **Net: Epic 19's social/auth foundation is de-risked at the contract level (RLS + RPC behave exactly as the app expects); the remaining gap is purely the on-device UI confirmation + the two logged code gaps (no `profiles` insert-on-signup; GoTrue no-MX email rejection).**
- **Epic 18 (Social Graph & Friends, v2.3) on-device verification (2026-06-24): FAIL found → fixed in-session → re-verified PASS, gate cleared.** On the SM-A520F (API 26, 1080×1920, `5200b2255ab59429`), Wi-Fi on. Two passes: (1) **no-key debug APK** (no `--dart-define`; empty Supabase + no RC key → free tier) and (2) **keyed debug APK** (`--dart-define-from-file=dart-defines.json` with the RC **Test Store** key `test_…`; Supabase still empty) to exercise the Pro-gated social surface. **The FAIL (Protocol B — Layout integrity):** as a Pro user, Social → **Amici** tab rendered `_FriendsShimmer` with **"BOTTOM OVERFLOWED BY 54 PIXELS"** on this device (`/private/tmp/pc18_keyed_friends_20260624.png`). Root cause: `_FriendsShimmer` (`lib/features/social/friends/presentation/pages/social_page.dart`) used a non-scrollable fixed-height `Column` (header 56 + 5×(60+12) ≈ 452 logical px) inside a nested `Scaffold`; on the SM-A520F's shorter logical height it exceeds the TabBarView slot. The shimmer is reachable in production on every cold-open of the Friends tab (initial/loading state) and permanently on load-error. Same class as the Epic 9 RPEInput (`9.1-WIDGET-005`) and Epic 16 SignInSheet overflows — not caught by the default 800px-tall `flutter_test` surface. **Fix (Paolo chose fix-and-reverify):** changed the `Column` to a `ListView` (matching the loaded `_FriendsList`, which already scrolls). `flutter analyze` clean on the file; `test/widget/social_page_test.dart` green (18.2-WIDGET-NEW-001/002). Rebuilt keyed + reinstalled; **re-verified PASS** — Amici shimmer now lays out and scrolls with no overflow stripe (`/private/tmp/pc18_keyed_friends_refix_20260624.png`). **Verified PASS (no-key, free):** Social tab present at index 3 in the bottom nav (18.1-AC5; `/private/tmp/pc18_social_free_20260624.png`); Social Pro gate → `_LockedBanner` ("Amici e social sono funzionalità Pro." + "Scopri Pro", no lock icon, no overflow); LockedBanner CTA → `ProUpsellSheet` (also confirmed the Epic-17 cooldown applies cross-entry — after a prior "non ora", the Social CTA is silently ignored, `pro_upsell_dismissed_date`); regression on Oggi, drawer, Impostazioni (Account/Abbonamento/Tema/Dispositivo/Dati), and the Epic-16 `SignInSheet` (no keyboard) all clean. **Verified PASS (keyed, Pro via a Test Store lifetime purchase):** Paywall loaded real RC offerings with the Epic-18-updated copy "Storico completo, tutti i grafici **e le funzioni social**" (`/private/tmp/pc18_keyed_paywall2_20260624.png`); the native "Test Store Purchase" AlertDialog driven via exact `uiautomator dump` bounds (not Flutter-coordinate estimates) → "Test valid purchase" → `SubscriptionBloc loaded(pro)` → Settings → Abbonamento flipped to "Gestisci abbonamento"; Social Pro chrome renders title "Sociale" + TabBar **Amici / Feed / Confronto** with working tab switching (`/private/tmp/pc18_keyed_feed_20260624.png`, `/private/tmp/pc18_keyed_comparison_20260624.png`). **N/A (no Supabase URL/anon key, no auth session — same precedent as Epic 16/17):** Story 18.1 handle setup + `VisibilityTierSelector` (only render in the Account page when `email != null`, i.e. signed in) and RLS privacy; all real 18.2 friend-request data (search/QR/contacts/accept/decline/remove), 18.3 activity-feed share/react/revoke, and 18.4 friends progress comparison data — these need a live Supabase EU backend + an authenticated session + (for read paths) Pro. Story 18.0 is test-only (no UI), covered by the suite. **Minor findings (NOT gate blockers — logged for the retro):** (1) **inconsistent error surfacing across social tabs** — the Amici tab shows the raw English exception `Failed to fetch friends: Bad state: No authenticated session` (`failure.message` is non-empty and shown verbatim), while Feed and Confronto show the localized "Qualcosa è andato storto. Riprova."; in production a Pro-but-not-signed-in user would hit the raw English string on Amici. (2) The `ProUpsellSheet` body copy is the generic Epic-17 "storico e grafici" fact even when entered from the Social locked banner (no social-specific copy). **Process/state note:** the device is left with the **fixed keyed build** installed and the RevenueCat **anonymous customer in Test Store Pro** (lifetime) state; this is reversible by clearing app data or reinstalling a no-key build (a no-key build can't read the entitlement → effective free). The RC Test Store key lives only in the gitignored `dart-defines.json`. **Outcome: FAIL fixed in-session and re-verified PASS → gate cleared, retrospective proceeds.**
- **Epic 17 RevenueCat live-flow follow-up (2026-06-23, same day): the previously-N/A IAP ACs were exercised on-device with a RevenueCat Test Store key and are now PASS.** Setup: RevenueCat project "Pulse Coach" (`proj8397e138`) with a **Test Store** SDK key `test_...` injected via `flutter build apk --debug --dart-define-from-file=pulse_coach/dart-defines.json` (gitignored), Wi-Fi on. Test Store products `monthly`/`yearly`/`lifetime` + offering `default` (current). **Entitlement mismatch found & fixed:** the dashboard entitlement had `lookup_key = "Pulse Coach Pro"` but `entitlement_gate.dart:20` checks `entitlements.active.containsKey('pro')` — so a purchase would not unlock Pro. Fixed correctly at the dashboard (not in code, preserving the documented `pro` production contract) via the **REST API v2**: created entitlement `pro` (`entlc01d59f113`) and attached all 3 products. **Now PASS:** **17.4-AC1** — paywall loads real offerings with store-fetched prices (Pro mensile 9,99 / annuale 79,98 / lifetime 99,99 US$, no hardcoded price; `/private/tmp/pc17_rc_recheck_20260623.png`). **17.4-AC2** — tapping "Acquista" presents the native RevenueCat **"Test Store Purchase"** AlertDialog (Test valid / Cancel / Test failed); "Test valid purchase" → `SubscriptionBloc` emits `loaded(pro)` → Settings → Abbonamento flips from "Scopri Pro" to **"Gestisci abbonamento"** (`/private/tmp/pc17_valid2_20260623.png`). Server-side confirmed via REST API: the anonymous customer (`$RCAnonymousID:063f4b7b…`) has `pro` (`entlc01d59f113`) **active**. **17.2 in-place unlock** — with the cohort set to `post_v2` (gated before), Progress unlocked the full Cronologia + Grafici in-place immediately after purchase, no celebration screen (`/private/tmp/pc17_ac2_unlocked_20260623.png`). **17.4-AC4** — Settings → "Gestisci abbonamento" launched the Google Play subscription-management page (foreground became `com.android.vending`; Play shows "Accedi" only because the device's Play account is unauthenticated — the `launchUrl` itself succeeded; `/private/tmp/pc17_ac4_manage_20260623.png`). **17.4-AC3 (restore): still residual** — the "Ripristina acquisti" button is present and wired, but a faithful cross-device restore needs a real store account; RevenueCat Test Store + anonymous users cannot demo a successful restore (a reinstall yields a new anonymous id with no associated transaction). **Tooling note (answers "why adb taps didn't open the dialog"):** the Test Store dialog is a native Android `AlertDialog`; `adb input tap` on its buttons needs exact bounds from `uiautomator dump` (buttons at y≈1403 on this 1080×1920 panel), not Flutter-screen coordinate estimates. **Process:** device DB restored to `pre_v2` (grandfathered, as-found) afterward; keyed debug build left installed at Paolo's request; Test Store prices left at defaults (not Epic-17 ACs; real prices are a Play Console concern); the temporary RevenueCat **secret** key was used only in-session from a non-repo path and is to be rotated/deleted by Paolo. This closes the Epic 17 IAP N/A items except AC3 (documented Test Store limitation).
- **Epic 17 (Pro Subscription & Feature Gating, v2.2) on-device verification (2026-06-23): PASS — gate cleared, no FAIL.** On the SM-A520F (API 26, 1080×1920, `5200b2255ab59429`), debug APK built fresh (no `--dart-define`, so empty `REVENUECAT_API_KEY_*` → RevenueCat observer mode per `main.dart:33-46`; device in airplane mode). The pre-existing install was correctly `install_cohort = 'pre_v2'` (the v9 migration `app_database.dart:118-142` backfills upgrading users as grandfathered). **Verified PASS:**
  - **17.2-AC1 (grandfathered) PASS** — with the as-found `pre_v2` profile, Progressi shows the FULL history: weekly-goal widget + Cronologia/Grafici tabs + 5 session rows (Mobilità/Cardio/Respirazione with RPE badges + abbandonata distinction), no gate. (`/private/tmp/pc17_progress_20260623.png`)
  - **17.2-AC2 (post-v2 free gate) PASS** — flipped `install_cohort` to `post_v2` in the DB (pulled via `run-as cat`, edited with host `sqlite3`, pushed back through `/data/local/tmp`; existing history kept to make the gate test stronger). Progressi then shows ONLY the WeeklyGoalIndicator + the single tappable prompt **"Lo storico completo è una funzione Pro."** (teal, underlined) — no tab bar, no history list, no charts, and **no lock icon / badge / 🔒 anywhere** (UX-DR25). (`/private/tmp/pc17_progress_free2_20260623.png`)
  - **17.2-AC3 + 17.3-AC1 (ProUpsellSheet) PASS** — tapping the prompt opens the bottom sheet (scrim dim) with the single fact "Accedi a tutto lo storico e ai grafici con PulseCoach Pro." + two buttons `non ora` (secondary) / `Scopri Pro` (primary). (`/private/tmp/pc17_upsell_20260623.png`)
  - **17.3-AC2/AC3/AC5 (cooldown) PASS** — `non ora` dismisses + records the cooldown (verified `flutter.pro_upsell_dismissed_date = 2026-06-23` written to `FlutterSharedPreferences.xml`); re-tapping the prompt is **silently ignored** (no sheet, no error), prompt still visible, no lock icon. (`/private/tmp/pc17_cooldown_20260623.png`)
  - **17.4 PaywallPage render + navigation PASS** — reached both via the sheet's `Scopri Pro` (after clearing the cooldown prefs key) and via Settings → **Abbonamento → "Scopri Pro"**. Page titled "PulseCoach Pro" with working back nav. (`/private/tmp/pc17_paywall_20260623.png`, `/private/tmp/pc17_settings_pro_20260623.png`)
  - **Settings → Account / Abbonamento sections PASS** — "Accedi al tuo account" (Epic 16) + "Scopri Pro" under Abbonamento render with no overflow; for a free user the Abbonamento row correctly shows "Scopri Pro" (not "Gestisci abbonamento"). (`/private/tmp/pc17_settings_20260623.png`)
  - **Regression PASS** — Today/Oggi (state bar "In recupero", CompletionRing 0/2, HeroSessionCard "Respirazione", PROSSIME Cardio), drawer (Profilo/Impostazioni/Privacy/Debug/AI Decision Log), Settings (Tema SegmentedButton Scuro/Chiaro/Sistema, no overflow) all render without regression or crash. (`/private/tmp/pc17_today_20260623.png`, `/private/tmp/pc17_drawer_20260623.png`)
  **N/A (not exercisable on this no-key autonomous build):** 17.4-AC1 (offerings + live price/billing-period display — offerings can't load without an RC key), 17.4-AC2 (purchase → entitlement), 17.4-AC3 ("Ripristina acquisti"), 17.4-AC4 ("Gestisci abbonamento" → platform management page — Pro-only, and as a free user the entry correctly shows "Scopri Pro" instead); the in-place Pro-unlock of Progress (17.2 "purchases Pro → history appears") also requires a live entitlement. **Minor finding (NOT a gate blocker — logged for the retro):** the Paywall error path surfaces the **raw RevenueCat SDK exception verbatim** ("There is no singleton instance… errors.rev.cat/configuring-sdk", English) because `EntitlementRepositoryImpl.getOfferings` returns `e.message` from the `PlatformException` and only falls back to the localized "Impossibile caricare gli abbonamenti." when `e.message` is null (`entitlement_repository_impl.dart:52-58`; the purchase path uses raw `e.toString()` at :84-85). This only manifests in the misconfigured/transient-error state (won't occur in a properly-keyed production build), it degrades gracefully with a `Riprova` button, no crash, free core unaffected (NFR34 holds) — but a transient production RC failure could still show an English/technical string. **Doc-drift finding (logged for the retro):** `epics.md` Epic 17 Story 17.2 AC2 still reads "only the most recent session **and** the current weekly goal widget are visible", but the delivered Story 17.2 file + implementation (`progress_page.dart:75-99`) show **only** the weekly-goal widget + prompt (no recent-session card); the story AC was refined but `epics.md` was not reconciled. Device matches the as-built story AC. **Process note:** DB and SharedPreferences were edited via `run-as` + host `sqlite3`/`sed` (same staging-through-`/data/local/tmp` technique as Epic 10), and **the original `pre_v2` DB was restored at the end** (re-verified `install_cohort = 'pre_v2'`), leaving the device in its as-found grandfathered state; device `/data/local/tmp` temp files cleaned up. **Outcome: PASS → gate cleared, retrospective proceeds.**
- **Epic 16 (Accounts & Authentication, v2.1) on-device verification (2026-06-22): FAIL found → fixed in-session → re-verified PASS, gate cleared.** **Fix:** `lib/features/auth/presentation/widgets/sign_in_sheet.dart` — wrapped the content `Column` in a `SingleChildScrollView` (kept the existing `viewInsets.bottom + 24` padding). Re-built + reinstalled; on the same SM-A520F, opening the soft keyboard on the email field no longer overflows — the sheet scrolls and Email/Password/Registrati/Accedi all stay reachable (screenshots `/private/tmp/pc16_reverify_kb_20260622.png`, `/private/tmp/pc16_reverify_scroll_20260622.png`). `flutter analyze` clean on the file. The original finding (kept for the record): On the SM-A520F (API 26, 1080×1920, `5200b2255ab59429`), debug APK (no `--dart-define` Supabase creds; device in airplane mode). **Verified PASS:** 16.1-AC4 — app launches with `currentSession == null`, Today/Oggi free core renders the full plan, no crash, no forced sign-in prompt, no FATAL in logcat (empty `SUPABASE_URL`/`SUPABASE_ANON_KEY` did not crash `Supabase.initialize`). Settings → new **Account** section ("Accedi al tuo account") renders above Tema/Dispositivo/Dati, no overflow. 16.2-AC1 — `SignInSheet` is a dismissible bottom sheet (drag handle + ✕) offering the three methods in App-Store order **Sign in with Apple → Accedi con Google → Accedi con Email** (email+password fields, "Registrati" toggle, "Accedi"); with the keyboard closed the sheet fits with no overflow. **FAIL (Protocol B — Layout integrity):** opening the soft keyboard to type the email/password makes the `SignInSheet` overflow — Flutter renders **"BOTTOM OVERFLOWED BY 179 PIXELS"** (screenshot `/private/tmp/pc16_emailclean_20260622.png`). Root cause: `lib/features/auth/presentation/widgets/sign_in_sheet.dart:42-49` — content is a `Column(mainAxisSize: min)` inside a `Padding` with `bottom: MediaQuery.viewInsets.bottom + 24` but **no `SingleChildScrollView`**; when the keyboard inset is added the column exceeds available height. Fix: wrap the content column in a `SingleChildScrollView` (sheet already shown with `isScrollControlled`-style sizing). This is a high-frequency path (any email sign-up/sign-in), so it blocks the gate. **N/A (not exercisable on this autonomous build):** real Apple/Google sign-in (16.2-AC3/AC4 — require Apple Developer Portal + Supabase dashboard provider config, explicitly manual per story dev notes), email confirmation/`unconfirmed` state (16.2-AC5), sign-out (16.2-AC6 — no live session), all of 16.3 Backup/Restore and 16.4 Delete/Export server flows (require live Supabase EU backend + Edge Functions + a signed-in session). The Account-gated Backup/Restore/Delete sub-screens were not reachable without an authenticated session. **Outcome: FAIL fixed in-session and re-verified PASS → gate cleared, retrospective proceeds.** (Paolo chose fix-and-reverify.) Screenshots: `/private/tmp/pc16_launch2_20260622.png` (Today free core), `/private/tmp/pc16_settings_20260622.png` (Account section), `/private/tmp/pc16_account_20260622.png` (SignInSheet, no kb), `/private/tmp/pc16_emailclean_20260622.png` (overflow with kb).
- **Epic 14 (Settings, Theme & Extras — FINAL epic, closes v1) on-device verification (2026-06-05): PASS.** On the SM-A520F (API 26, 1080×1920, `5200b2255ab59429`), debug APK, suite at 854/854 green. All user-reachable Epic 14 ACs verified per the On-Device UI Verification Protocol. **Story 14.1 (theme):** AC1 `SegmentedButton` shows "Scuro / Chiaro / Sistema" with **no overflow** (the known `Strengt`/`h` profile-segment defect did NOT recur here); AC2 Sistema follows device (light device → light app); AC3 dark `#0F1119` surface; AC4 light theme via `ColorScheme.fromSeed()` applied immediately; AC5 force-stop + relaunch preserved "Scuro" (SharedPreferences persistence). **Story 14.2 (privacy):** AC1 all 4 sections present (Dati sul dispositivo / API Salute / Posizione / Conformità GDPR); AC2 copy states "non trasmette dati a server esterni", no external-account/server references. **Story 14.3 (device & sync):** AC1 all 4 info sections render (API Salute, WearOS, Sincronizzazione, Cache); **AC2 N/A (platform)** — Health Connect is unavailable on API 26, so no permission dialog fires and status stays `unknown` (correct degradation, not a defect); AC3 "Sincronizza ora" correctly absent (sync queue empty). **Story 14.4 (AI decision log):** AC1 list renders 6 decisions (date, arm, RPE); AC2 row-tap expands `decidedAt`/`armKey`/`rpeValue`/`stateVector`; **AC3 N/A** — empty state not exercisable with data present; AC4 visible in `kDebugMode` build. **Story 14.5 (data export):** AC1 "Dati" section + "Esporta dati" tile; AC2 bottom sheet with JSON/CSV; AC3 JSON → OS share sheet ("Completa azione usando"); AC4 CSV → same share sheet; AC5 loading state — export fast, no hang. The 14.5-AC3/AC4 PARTIAL gap in the traceability matrix (share_plus platform channel not unit-testable) is closed on-device here. **Regression:** Today (Oggi — plan, state bar, CompletionRing, bottom nav), Sessioni (shimmer), Progressi (Cronologia/Grafici tabs, shimmer) all PASS, no crash. Two legitimate N/A only (Health Connect on API 26; empty state with data present). This closes E14R-1 ("verification done but record slips" recurring pattern, ref E13R-1/Epic 11).
- **Epic 13 offline on-device smoke (2026-06-04): PASS — all checks pass.** On the SM-A520F (API 26, 1080×1920, `5200b2255ab59429`), the Story 13.1 offline-first user scenarios (AC1–AC4) were exercised on device in airplane mode: plan generation succeeds with zero error states (cached exercises + indoor default), session + RPE persist locally, Progress renders from local data (no "offline" error message), and mid-session network loss is transparent. All PASS per the On-Device UI Verification Protocol. Backing logic is also covered by the unit/integration suite (809/809 green: `test/offline/offline_core_features_test.dart`, `test/core/sync/sync_manager_test.dart`, `test/core/database/data_persistence_test.dart`). Epic 13 introduced no schema change (the `sync_queue` table already existed at schema v8) and no new ARB keys.
- **Epic 12 WearOS on-device verification (2026-06-04): PASS — full real end-to-end, physical phone paired to the Wear emulator.** Setup: SM-A520F (API 26, `5200b2255ab59429`) paired to the `Wear_OS_Large_Round` AVD (API 36, round 454×454, `emulator-5554`) via the Wear OS companion app ("Collegato all'emulatore") + `adb -s <phone> forward tcp:5601 tcp:5601`. Both the phone app and the wear app share `applicationId com.pulsecoach.pulse_coach` (correct for Wear companions) signed with the same debug key, so the `watch_connectivity` Data Layer routes between them. The phone UI was driven entirely over adb (taps + screenshots). All ACs verified live:
  - **12.1** idle home renders "PulseCoach Wear" text + "Tap me" button on the round display.
  - **12.2 AC1** watch shows the live step name + countdown ("Riscaldamento / 01:54", "Mobilità / 05:32") broadcast from the phone session. HR row absent — the SM-A520F provides no live HR ("when available" → correct, not a defect).
  - **12.2 AC2** timer sync within 1s — multiple samples matched the phone (01:54 vs 01:55; 05:32 vs 05:32; paused-at-04:51 vs 04:51).
  - **12.2 AC3** step transition reflected on the watch — went from "Riscaldamento" to "Mobilità" as the phone advanced steps.
  - **12.3 AC1** on abandon, the phone moved to the RPE screen and the watch showed the summary "Mobility / 10 min / Valuta RPE sul telefono / (abbandonata)" (error-colored abandoned indicator).
  - **12.3 AC2** on RPE submit, the watch returned to the idle home (verified twice).
  - **12.4 AC1** disconnect resilience — killed the emulator mid-session (`adb emu kill`); the phone session continued uninterrupted (timer kept counting 01:55→00:50→…, progress bar advancing, no error/pause/crash).
  - **12.4 AC2** reconnect resume — rebooted the emulator + relaunched the wear app; it hydrated from the (continuously phone-updated) application context and resumed showing the CURRENT step ("Mobilità / 05:32"), not the stale pre-disconnect step.
  Layout/theme on the round 454×454 display: no overflow/clipping, monospace timer, teal-on-dark theme, content inside the round safe zone. Note: `pm clear` does NOT clear the Wear Data Layer application context (it persists in Play Services) — a stale summary survived an app-data wipe and re-hydrated on launch; the clean reset path is an end message (RPE submit) or the phone overwriting the context with a newer payload. Backing logic also covered by 13 wear widget tests + 16 phone `session_wear_bridge_service_test.dart` tests, all green; `flutter analyze` clean in `wear/`. **This closes the Epic 12 transport gap and the E11R-PREP WearOS-environment item.**
- **Epic 11 on-device verification (2026-06-02): PARTIAL — phone PASS, tablet NOT verified.** On the SM-A520F (API 26, 1080x1920 — a phone), the phone-scoped Epic 11 ACs were verified PASS: Story 11.1 phone `BottomNavigationBar` path (sub-600dp, unchanged), Story 11.3 `InSessionView` portrait + landscape two-column layout and Today landscape behaviour (no overflow/regression). **The tablet-scoped ACs were NOT verified** because no tablet/large-screen device was available: Story 11.1 `NavigationRail` rendering + label-overflow band 600–700dp ("Sessioni"/"Progressi" at `labelType.all`, `minWidth: 80`), and Story 11.2 tablet master-detail + flex 40/60 split (11.2-AC4). Consequently the deferred items **11.1 NavigationRail label-band** and **11.2-AC4 flex ratio** remain OPEN pending a tablet/large-screen device or AVD (tracked as Epic 11 retro action E11R-3, same hardware-availability theme as the Epic 12 WearOS environment prep E11R-PREP). The Epic 11 quality gate is PASS on widget tests (759 total, P1 100%); the tablet visual confirmation is the residual gap.
- **Epic 10 on-device verification (2026-05-27): PASS** on the SM-A520F (API 26, 1080x1920). All Progress-tab ACs verified per the On-Device UI Verification Protocol — Story 10.1 (history timeline: empty / shimmer-loading / populated reverse-chronological rows / abandoned distinction), Story 10.2 (insufficient-data placeholder + all 4 `fl_chart` charts: minutes/week bar, completion-rate ring 89%, RPE-trend line, session-type donut), Story 10.3 (weekly goal 0/3 empty + 3/3 filled "Obiettivo raggiunto!"). No FAIL; no regression on Today (Epic 7) or Sessioni (Epic 6.3). Populated states were verified by seeding completed `session_logs` + `rpe_feedback` rows directly into `app_flutter/pulse_coach_db.sqlite` via `run-as` (data staged through `/data/local/tmp`, which the app uid can read — note `/sdcard` is NOT readable by the app uid on this device), then restoring the original DB afterward. The empty-state-illustration ambiguity in the old epics.md Story 10.1 AC was resolved as **text-only, no illustration** (per ux-design-specification.md §1424); epics.md AC wording corrected to match.

## Test / build baseline history (newest first)

- As of the Epic 9 on-device verification (2026-05-24), `flutter test` passes with **685/685** tests. (+1 from the prior Epic 9 baseline of 684: `9.1-WIDGET-005` regression — RPEInput must not overflow and must keep all 10 targets on-screen at a 360dp phone width. This closed an on-device FAIL where the single 10-target row overflowed by 204dp on the SM-A520F, clipping RPE 8/9/10 off-screen; `RPEInputWidget` now degrades to two rows of five below ~516dp available width while preserving the ≥48dp hit area. Single-row-at-360dp was caught only on device because the default flutter_test surface is 800dp wide. The two-row degrade is provisionally ratified in epics.md Story 9.1 AC1 and ux-design-specification.md UX-DR9 via Sprint Change Proposal 2026-05-24; formal Sally+John sign-off tracked as E9R-3.)
- As of the Story 8.2 code-review reconciliation (2026-05-17), `flutter test` passes with **566/566** tests. (+50 from Story 7.5.1 baseline of 516: Stories 8.0/8.1/8.2 added DAO + CountdownOverlay + InSessionView coverage including the new `8.2-CUBIT-007` 00:00-frame regression.)
- As of the Story 7.5.1 code-review reconciliation (2026-05-16), `flutter test` passes with **516/516** tests. (+13 from the Epic 7 closure baseline of 503: Story 7.0 Failure-equality regression test + traceability matrix / TEA / TodaySessionCubit tests landed during Epic 7.5 setup.)
- As of the Epic 6.5 closure (2026-05-15), `flutter test` passed with `388/388` tests.
- As of the last device check, `flutter build apk --debug` passed and produced:
  `pulse_coach/build/app/outputs/flutter-apk/app-debug.apk`.
- `flutter analyze` is clean: **0 issues** (Story 6.5.1 closed the legacy 38-issue backlog; zero-tolerance policy now enforceable).
- Core direct dependencies upgraded to latest resolvable majors via Story 6.5.4 (go_router 17, get_it 9, injectable 3, geolocator 14, sensors_plus 7, fl_chart 1.2, google_fonts 8).
