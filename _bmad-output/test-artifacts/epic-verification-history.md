# Epic Verification & Test Baseline History

Historical, per-epic on-device/emulator verification records and test/build baselines,
moved out of the project `CLAUDE.md` to keep it lean. This file is referenced by the
`bmad-retrospective` workflow (team override `_bmad/custom/bmad-retrospective.toml`) so the
agent has the prior epics' verification context when running a retrospective.

Append a new dated entry here after each epic's on-device/emulator verification gate.
The current live baseline (test command, build command) stays in `CLAUDE.md`.

## On-device / emulator verification records (newest first)

- **Epic 14 (Settings, Theme & Extras — FINAL epic, closes v1) on-device verification (2026-06-05): PASS.** On the SM-A520F (API 26, 1080×1920, `5200b2255ab59429`), debug APK, suite at 854/854 green. All user-reachable Epic 14 ACs verified per the On-Device UI Verification Protocol. **Story 14.1 (theme):** AC1 `SegmentedButton` shows "Scuro / Chiaro / Sistema" with **no overflow** (the known `Strengt`/`h` profile-segment defect did NOT recur here); AC2 Sistema follows device (light device → light app); AC3 dark `#0F1119` surface; AC4 light theme via `ColorScheme.fromSeed()` applied immediately; AC5 force-stop + relaunch preserved "Scuro" (SharedPreferences persistence). **Story 14.2 (privacy):** AC1 all 4 sections present (Dati sul dispositivo / API Salute / Posizione / Conformità GDPR); AC2 copy states "non trasmette dati a server esterni", no external-account/server references. **Story 14.3 (device & sync):** AC1 all 4 info sections render (API Salute, WearOS, Sincronizzazione, Cache); **AC2 N/A (platform)** — Health Connect is unavailable on API 26, so no permission dialog fires and status stays `unknown` (correct degradation, not a defect); AC3 "Sincronizza ora" correctly absent (sync queue empty). **Story 14.4 (AI decision log):** AC1 list renders 6 decisions (date, arm, RPE); AC2 row-tap expands `decidedAt`/`armKey`/`rpeValue`/`stateVector`; **AC3 N/A** — empty state not exercisable with data present; AC4 visible in `kDebugMode` build. **Story 14.5 (data export):** AC1 "Dati" section + "Esporta dati" tile; AC2 bottom sheet with JSON/CSV; AC3 JSON → OS share sheet ("Completa azione usando"); AC4 CSV → same share sheet; AC5 loading state — export fast, no hang. The 14.5-AC3/AC4 PARTIAL gap in the traceability matrix (share_plus platform channel not unit-testable) is closed on-device here. **Regression:** Today (Oggi — plan, state bar, CompletionRing, bottom nav), Sessioni (shimmer), Progressi (Cronologia/Grafici tabs, shimmer) all PASS, no crash. Two legitimate N/A only (Health Connect on API 26; empty state with data present). This closes E14R-1 ("verification done but record slips" recurring pattern, ref E13R-1/Epic 11).
- **Epic 13 offline on-device smoke (2026-06-04): PASS — all checks pass.** On the SM-A520F (API 26, 1080×1920, `5200b2255ab59429`), the Story 13.1 offline-first user scenarios (AC1–AC4) were exercised on device in airplane mode: plan generation succeeds with zero error states (cached exercises + indoor default), session + RPE persist locally, Progress renders from local data (no "offline" error message), and mid-session network loss is transparent. All PASS per the On-Device UI Verification Protocol. Backing logic is also covered by the unit/integration suite (809/809 green: `test/offline/offline_core_features_test.dart`, `test/core/sync/sync_manager_test.dart`, `test/core/database/data_persistence_test.dart`). Epic 13 introduced no schema change (the `sync_queue` table already existed at schema v8) and no new ARB keys.
- **Epic 12 WearOS on-device verification (2026-06-04): PASS — full real end-to-end, physical phone paired to the Wear emulator.** Setup: SM-A520F (API 26, `5200b2255ab59429`) paired to the `Wear_OS_Large_Round` AVD (API 36, round 454×454, `emulator-5554`) via the Wear OS companion app ("Collegato all'emulatore") + `adb -s <phone> forward tcp:5601 tcp:5601`. Both the phone app and the wear app share `applicationId com.pulsecoach.pulse_coach` (correct for Wear companions) signed with the same debug key, so the `watch_connectivity` Data Layer routes between them. The phone UI was driven entirely over adb (taps + screenshots). All ACs verified live:
  - **12.1** idle home renders "PulseCoach Wear" text + "Tap me" button on the round display.
  - **12.2 AC1** watch shows the live step name + countdown ("Riscaldamento / 01:54", "Mobilità / 05:32") broadcast from the phone session. HR row absent — the SM-A520F provides no live HR ("when available" → correct, not a defect).
  - **12.2 AC2** timer sync within 1s — multiple samples matched the phone (01:54 vs 01:55; 05:32 vs 05:32; paused-at-04:51 vs 04:51).
  - **12.2 AC3** step transition reflected on the watch — went from "Riscaldamento" to "Mobilità" as the phone advanced steps.
  - **12.3 AC1** on abandon, the phone moved to the RPE screen and the watch showed the summary "Mobility / 10 min / Valuta RPE sul telefono / (abbandonata)" (error-colored abandoned indicator).
  - **12.3 AC2** on RPE submit, the watch returned to the idle home (verified twice).
  - **12.4 AC1** disconnect resilience — killed the emulator mid-session (`adb emu kill`); the phone session continued uninterrupted (timer kept counting 01:55→00:50→…, progress bar advancing, no error/pause/crash).
  - **12.4 AC2** reconnect resume — rebooted the emulator + relaunched the wear app; it hydrated from the (continuously phone-updated) application context and resumed showing the CURRENT step ("Mobilità / 05:32"), not the stale pre-disconnect step.
  Layout/theme on the round 454×454 display: no overflow/clipping, monospace timer, teal-on-dark theme, content inside the round safe zone. Note: `pm clear` does NOT clear the Wear Data Layer application context (it persists in Play Services) — a stale summary survived an app-data wipe and re-hydrated on launch; the clean reset path is an end message (RPE submit) or the phone overwriting the context with a newer payload. Backing logic also covered by 13 wear widget tests + 16 phone `session_wear_bridge_service_test.dart` tests, all green; `flutter analyze` clean in `wear/`. **This closes the Epic 12 transport gap and the E11R-PREP WearOS-environment item.**
- **Epic 11 on-device verification (2026-06-02): PARTIAL — phone PASS, tablet NOT verified.** On the SM-A520F (API 26, 1080x1920 — a phone), the phone-scoped Epic 11 ACs were verified PASS: Story 11.1 phone `BottomNavigationBar` path (sub-600dp, unchanged), Story 11.3 `InSessionView` portrait + landscape two-column layout and Today landscape behaviour (no overflow/regression). **The tablet-scoped ACs were NOT verified** because no tablet/large-screen device was available: Story 11.1 `NavigationRail` rendering + label-overflow band 600–700dp ("Sessioni"/"Progressi" at `labelType.all`, `minWidth: 80`), and Story 11.2 tablet master-detail + flex 40/60 split (11.2-AC4). Consequently the deferred items **11.1 NavigationRail label-band** and **11.2-AC4 flex ratio** remain OPEN pending a tablet/large-screen device or AVD (tracked as Epic 11 retro action E11R-3, same hardware-availability theme as the Epic 12 WearOS environment prep E11R-PREP). The Epic 11 quality gate is PASS on widget tests (759 total, P1 100%); the tablet visual confirmation is the residual gap.
- **Epic 10 on-device verification (2026-05-27): PASS** on the SM-A520F (API 26, 1080x1920). All Progress-tab ACs verified per the On-Device UI Verification Protocol — Story 10.1 (history timeline: empty / shimmer-loading / populated reverse-chronological rows / abandoned distinction), Story 10.2 (insufficient-data placeholder + all 4 `fl_chart` charts: minutes/week bar, completion-rate ring 89%, RPE-trend line, session-type donut), Story 10.3 (weekly goal 0/3 empty + 3/3 filled "Obiettivo raggiunto!"). No FAIL; no regression on Today (Epic 7) or Sessioni (Epic 6.3). Populated states were verified by seeding completed `session_logs` + `rpe_feedback` rows directly into `app_flutter/pulse_coach_db.sqlite` via `run-as` (data staged through `/data/local/tmp`, which the app uid can read — note `/sdcard` is NOT readable by the app uid on this device), then restoring the original DB afterward. The empty-state-illustration ambiguity in the old epics.md Story 10.1 AC was resolved as **text-only, no illustration** (per ux-design-specification.md §1424); epics.md AC wording corrected to match.

## Test / build baseline history (newest first)

- As of the Epic 9 on-device verification (2026-05-24), `flutter test` passes with **685/685** tests. (+1 from the prior Epic 9 baseline of 684: `9.1-WIDGET-005` regression — RPEInput must not overflow and must keep all 10 targets on-screen at a 360dp phone width. This closed an on-device FAIL where the single 10-target row overflowed by 204dp on the SM-A520F, clipping RPE 8/9/10 off-screen; `RPEInputWidget` now degrades to two rows of five below ~516dp available width while preserving the ≥48dp hit area. Single-row-at-360dp was caught only on device because the default flutter_test surface is 800dp wide. The two-row degrade is provisionally ratified in epics.md Story 9.1 AC1 and ux-design-specification.md UX-DR9 via Sprint Change Proposal 2026-05-24; formal Sally+John sign-off tracked as E9R-3.)
- As of the Story 8.2 code-review reconciliation (2026-05-17), `flutter test` passes with **566/566** tests. (+50 from Story 7.5.1 baseline of 516: Stories 8.0/8.1/8.2 added DAO + CountdownOverlay + InSessionView coverage including the new `8.2-CUBIT-007` 00:00-frame regression.)
- As of the Story 7.5.1 code-review reconciliation (2026-05-16), `flutter test` passes with **516/516** tests. (+13 from the Epic 7 closure baseline of 503: Story 7.0 Failure-equality regression test + traceability matrix / TEA / TodaySessionCubit tests landed during Epic 7.5 setup.)
- As of the Epic 6.5 closure (2026-05-15), `flutter test` passed with `388/388` tests.
- As of the last device check, `flutter build apk --debug` passed and produced:
  `pulse_coach/build/app/outputs/flutter-apk/app-debug.apk`.
- `flutter analyze` is clean: **0 issues** (Story 6.5.1 closed the legacy 38-issue backlog; zero-tolerance policy now enforceable).
- Core direct dependencies upgraded to latest resolvable majors via Story 6.5.4 (go_router 17, get_it 9, injectable 3, geolocator 14, sensors_plus 7, fl_chart 1.2, google_fonts 8).
